import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click the Baby!</h1>
    <h3 className="text-center">Click a Baby to earn points, but don't click the same baby more than once!</h3>
  </div>
)
export default Header;